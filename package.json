{
  "name": "libroconapi",
  "version": "1.0.0",
  "main": "servidor.js",
  "scripts": {
"dev": "concurrently \"nodemon servidor.js --ext js,ejs,json\" \"browser-sync start --proxy 'http://localhost:3000' --port 3001 --files 'views/**/*.ejs, public/css/**/*.css, public/js/**/*.js' --no-open --no-notify\"",
    "start": "node servidor.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}